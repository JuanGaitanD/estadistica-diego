# src/components/layout

Componentes estructurales de la aplicación (proveedores globales, shell de
página, navegación). Incluye `theme-provider.tsx`, el envoltorio de
`next-themes` usado en `src/app/layout.tsx`.
